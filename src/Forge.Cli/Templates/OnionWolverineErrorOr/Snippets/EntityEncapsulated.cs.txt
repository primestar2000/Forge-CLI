// forge:tokens Usings, Namespace, Entity, BaseClass, Properties, Constructor, Update
{{Usings}}namespace {{Namespace}};

public class {{Entity}}{{BaseClass}}
{
{{Properties}}

    /// <summary>
    /// Required by EF Core to materialise {{Entity}} from the database. Not for application
    /// code — construct through the public constructor so the entity is always complete.
    /// </summary>
    private {{Entity}}()
    {
    }
{{Constructor}}{{Update}}
}
