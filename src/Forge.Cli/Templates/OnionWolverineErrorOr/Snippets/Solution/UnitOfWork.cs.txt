// forge:tokens Namespace, UnitOfWorkInterfaceNamespace, DbContextName, DbContextNamespace
using {{DbContextNamespace}};
using {{UnitOfWorkInterfaceNamespace}};

namespace {{Namespace}};

/// <summary>
/// 'forge make:repo -i &lt;Entity&gt;' adds a property, a constructor parameter and its
/// assignment here automatically. Keep the constructor body-assignment shape.
/// </summary>
public class UnitOfWork : IUnitOfWork
{
    private readonly {{DbContextName}} _context;

    public UnitOfWork({{DbContextName}} context)
    {
        _context = context;
    }

    public Task<int> SaveChangesAsync(CancellationToken cancellationToken = default)
    {
        return _context.SaveChangesAsync(cancellationToken);
    }
}
