<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>{{TargetFramework}}</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="ErrorOr" Version="2.0.1" />
    <!-- SQLite, not InMemory: InMemory is not a relational provider, so it cannot resolve
         IMigrator and every db:* command fails with "no database provider has been configured".
         SQLite needs no server, creates its file on demand, and supports migrations - so the
         scaffold is both runnable AND migratable with no setup. Swap for SqlServer/Npgsql later;
         a provider from the current generation will resolve, which is the point of the matched
         versions here. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="{{EfCoreVersion}}" />
    <!-- Pinned to override the transitive SQLitePCLRaw 2.1.x, whose 2.1.6 carries a known
         high-severity advisory (NU1903 / GHSA-2m69-gcr7-jv3q). Scaffolding a vulnerable
         dependency into every new solution is not acceptable. Caught by the compile gate's
         zero-warning policy. -->
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="{{SqlitePclRawVersion}}" />
    <!-- The EF Core tools require Design on the STARTUP project, which is this one. Infrastructure
         references it with PrivateAssets=all so it does not flow through - without this, every
         db:* command fails with "Your startup project doesn't reference ...Design". -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="{{EfCoreVersion}}">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="WolverineFx" Version="{{WolverineVersion}}" />
{{SwaggerReference}}{{ForgeRuntimeReference}}
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="../{{Solution}}.Infrastructure/{{Solution}}.Infrastructure.csproj" />
  </ItemGroup>

</Project>
