// Tokens: Namespace, RoleEnum, RoleEnumNamespace, ApplicationAuthNamespace
using {{ApplicationAuthNamespace}};
using {{RoleEnumNamespace}};

namespace {{Namespace}};

/// <summary>
/// Scoped implementation. Registered as ICurrentUser AND ICurrentUserSetter against the same
/// instance so the middleware can set it and handlers can only read it.
/// </summary>
public sealed class CurrentUser : ICurrentUserSetter
{
    public Guid Id { get; private set; }

    public string Email { get; private set; } = string.Empty;

    public {{RoleEnum}} Role { get; private set; } = {{RoleEnum}}.Guest;

    public bool IsAuthenticated { get; private set; }

    public bool IsAdmin() => Role == {{RoleEnum}}.Admin;

    public void SetUser(Guid id, string email, {{RoleEnum}} role)
    {
        Id = id;
        Email = email;
        Role = role;
        IsAuthenticated = true;
    }
}
